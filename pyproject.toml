[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "atmozero"
version = "0.1.0"
description = "Self-play post-training for caption-free weather time-series captioning"
authors = [{name = "Anonymous"}]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.2",
    "transformers>=4.45",
    "verl>=0.4",
    "vllm>=0.6",
    "ray>=2.10",
    "accelerate>=0.34",
    "datasets>=2.21",
    "xarray>=2024.6",
    "zarr>=2.17",
    "gcsfs>=2024.6",
    "numpy>=1.26",
    "pandas>=2.2",
    "scipy>=1.13",
    "scikit-learn>=1.5",
    "pyyaml>=6.0",
    "tqdm>=4.66",
    "wandb>=0.18",
]

[tool.setuptools.packages.find]
include = ["atmozero*"]
